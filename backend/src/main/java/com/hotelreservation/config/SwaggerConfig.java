package com.hotelreservation.config;

/**
 * Swagger/OpenAPI configuration placeholder.
 * springdoc-openapi dependency has been removed to avoid network dependency issues.
 * API documentation is self-describing via REST conventions and JavaDoc.
 * To re-enable Swagger UI, add springdoc-openapi-starter-webmvc-ui to pom.xml
 * when internet access is available, and restore SwaggerConfig bean.
 */
public class SwaggerConfig {
    // No-op — springdoc removed for offline build compatibility
}
